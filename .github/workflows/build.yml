name: Build & Release

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # ─────────────────────────────────────────────
  # Job 1: 构建服务端二进制（多平台交叉编译）
  # ─────────────────────────────────────────────
  build-server:
    name: Server / ${{ matrix.goos }}-${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
            ext: .exe

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build server binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          OUTPUT="dist/kite-proxy-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }}"
          go build \
            -ldflags="-s -w -X main.version=${{ github.ref_name }}" \
            -o "${OUTPUT}" \
            .

      - name: Upload server artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/
          if-no-files-found: error

  # ─────────────────────────────────────────────
  # Job 2: 构建 Wails 桌面应用 — Windows
  # ─────────────────────────────────────────────
  build-desktop-windows:
    name: Desktop / windows-amd64
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Cache Wails build assets (WebView2 etc.)
        uses: actions/cache@v4
        with:
          path: |
            ~\AppData\Local\wails
            ~\AppData\Roaming\wails
          key: wails-windows-${{ runner.os }}-${{ hashFiles('go.sum') }}
          restore-keys: |
            wails-windows-${{ runner.os }}-

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        working-directory: ui
        run: npm ci

      - name: Pre-download WebView2 fixed distribution
        shell: pwsh
        run: |
          $wv2Url = "https://github.com/nicholasgasior/gsfmt/releases/download/v0.0.0-skip/skip"
          # 直接用 Wails 内置路径预热，让 wails build 不再中途下载
          $wailsCacheDir = "$env:LOCALAPPDATA\wails"
          if (-not (Test-Path $wailsCacheDir)) { New-Item -ItemType Directory -Force $wailsCacheDir | Out-Null }
          # 下载 WebView2 bootstrapper (小文件，用于触发 fixed distribution 缓存)
          $bootstrapUrl = "https://go.microsoft.com/fwlink/p/?LinkId=2124703"
          $bootstrapOut = "$wailsCacheDir\MicrosoftEdgeWebview2Setup.exe"
          if (-not (Test-Path $bootstrapOut)) {
            try {
              Invoke-WebRequest -Uri $bootstrapUrl -OutFile $bootstrapOut -TimeoutSec 60
            } catch {
              Write-Host "Bootstrap download skipped (non-fatal): $_"
            }
          }

      - name: Build desktop app (Windows)
        shell: pwsh
        env:
          CGO_ENABLED: "1"
        run: wails build -ldflags "-X main.version=${{ github.ref_name }}"

      - name: Package with NSIS (optional)
        shell: pwsh
        run: |
          $nsis = Get-Command makensis -ErrorAction SilentlyContinue
          if ($nsis) {
            wails build -nsis -ldflags "-X main.version=${{ github.ref_name }}"
          } else {
            Write-Host "NSIS not found, skipping installer packaging"
          }
        continue-on-error: true

      - name: Upload Windows desktop artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-windows
          path: build/bin/
          if-no-files-found: error

  # ─────────────────────────────────────────────
  # Job 3: 构建 Wails 桌面应用 — macOS (universal)
  # ─────────────────────────────────────────────
  build-desktop-macos:
    name: Desktop / macos-universal
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        working-directory: ui
        run: npm ci

      - name: Build desktop app (macOS universal)
        run: wails build -platform darwin/universal -ldflags "-X main.version=${{ github.ref_name }}"

      - name: Package as DMG
        run: |
          APP_PATH=$(find build/bin -name "*.app" | head -1)
          hdiutil create -volname "Kite Proxy" \
            -srcfolder "${APP_PATH}" \
            -ov -format UDZO \
            build/bin/kite-proxy-macos-universal.dmg

      - name: Upload macOS desktop artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-macos
          path: build/bin/*.dmg
          if-no-files-found: error

  # ─────────────────────────────────────────────
  # Job 4: 构建 Wails 桌面应用 — Linux
  # ─────────────────────────────────────────────
  build-desktop-linux:
    name: Desktop / linux-amd64
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            nsis \
            pkg-config

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        working-directory: ui
        run: npm ci

      - name: Build desktop app (Linux)
        run: wails build -ldflags "-X main.version=${{ github.ref_name }}"

      - name: Upload Linux desktop artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-linux
          path: build/bin/
          if-no-files-found: error

  # ─────────────────────────────────────────────
  # Job 5: 创建 Release（仅在推送 tag 时触发）
  # ─────────────────────────────────────────────
  release:
    name: Create GitHub Release
    needs:
      - build-server
      - build-desktop-windows
      - build-desktop-macos
      - build-desktop-linux
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: false

      - name: Prepare release assets
        run: |
          mkdir -p release
          find artifacts/ -type f | while read f; do
            cp "$f" release/
          done
          ls -lh release/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Kite Proxy ${{ github.ref_name }}"
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: release/*
